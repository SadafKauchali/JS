// Arrow function to check if a person is an adult
const isAdult = (age) => age >= 18;

// Example usage
console.log(isAdult(20)); // true
console.log(isAdult(17)); // false
console.log(isAdult(18)); // true
